# Screenshots

Product screenshots used in README, Chinese docs, and the landing page (`preview/`).

| File | Description |
|------|-------------|
| `home-en.png` | English UI, light mode, empty chat + suggestion chips |
| `home-zh.png` | Chinese UI, light mode, empty chat + suggestion chips |

## Landing page

Copies also live at `preview/assets/screenshots/` for GitHub Pages (self-contained site).

When you replace a shot:

1. Update `docs/screenshots/*.png`
2. Copy to `preview/assets/screenshots/`
3. Commit both so docs + site stay in sync
